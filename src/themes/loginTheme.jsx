// src/themes/loginTheme.js
import { createTheme } from "@mui/material/styles";

const loginTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#00aaff",
    },
    background: {
      default: "#f5f5f5",
    },
  },
});

export default loginTheme;
